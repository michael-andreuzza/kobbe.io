---
interface Props {
  variant?: "nav" | "prose" | "default" | "narrow" | "wide";
  class?: string; // Optional prop for additional classes
  id?: string; // Optional prop for an id
}
const { variant = "default", class: extraClass = "", id } = Astro.props;
// Map each variant to its specific classes
const variantClasses: Record<NonNullable<Props["variant"]>, string> = {
  nav: "mx-auto max-w-280 px-8",
  // Grids
  default: "mx-auto max-w-280 px-8",
  wide: "mx-auto px-8",
  narrow: "mx-auto max-w-150 px-8  ",

  prose: [
    // Base
    "prose",
    "text-sm",
    "2xl:text-base",
    "max-w-none",
    "text-muted-foreground",
    // Headings
    "prose-headings:font-normal",
    // Softened a step below pure foreground so headings on the inverted
    // docs panel don't glare white, while staying above the muted body text.
    "prose-headings:text-foreground/80",
    "prose-headings:first:mt-0",
    // Paragraphs
    "prose-p:muted-foreground",
    // Blockquotes
    "prose-blockquote:border-border",
    "prose-blockquote:text-muted-foreground",
    "prose-blockquote:font-normal",
    "prose-blockquote:tracking-normal",
    // Links
    "prose-a:text-muted-foreground",
    "prose-a:duration-200",
    // Images
    "prose-img:border",
    "prose-img:border-border",
    // HR
    "prose-hr:border-border",
    // Tables
    // Typography plugin draws its own table borders from these vars;
    // point them at the semantic border token so dividers match.
    "[--tw-prose-th-borders:var(--border)]",
    "[--tw-prose-td-borders:var(--border)]",
    "prose-table:w-full",
    "prose-table:border-collapse",
    "prose-table:border-0",
    "prose-thead:border-b",
    "prose-thead:border-border",
    "prose-tbody:divide-y",
    "prose-tbody:divide-border",
    "[&_tbody_tr]:transition-colors",
    "[&_tbody_tr:hover]:bg-muted/30",
    "prose-th:border-0",
    "prose-th:py-2.5",
    "prose-th:pr-4",
    "prose-th:text-xs",
    "prose-th:font-semibold",
    "prose-th:tracking-wide",
    "prose-th:text-muted-foreground",
    "prose-td:border-0",
    "prose-td:py-2.5",
    "prose-td:pr-4",
    "prose-td:align-top",
    "prose-td:text-muted-foreground",
    "[&_td_code]:rounded-md",
    "[&_td_code]:bg-card",
    "[&_td_code]:px-1.5",
    "[&_td_code]:py-0.5",
    // Code & Pre
    "prose-code:font-medium",
    "prose-code:text-foreground",
    "prose-strong:font-semibold",
    "prose-strong:text-foreground/80",
    "prose-pre:bg-card",
    "prose-pre:text-foreground",
    "prose-pre:rounded-md",
    "prose-pre:scrollbar-hide prose-pre:border-none",
  ].join(" "),
};
// Combine the classes for the specified variant with any extra classes
const classes = `${variantClasses[variant]} ${extraClass}`.trim();
---

<div class={classes} id={id}>
  <slot />
</div>
