---
/**
 * Closer in Kobbe's own voice: a quiet CTA on paper, then a muted footer
 * with the wordmark set as large as the viewport allows.
 */
import Logo from "@/components/assets/Logo.astro";
import Wordmark from "@/components/assets/Wordmark.astro";
import Wrapper from "@/components/foundations/containers/Wrapper.astro";
import { APP_SIGNUP_URL } from "@/lib/site-mega-menu";
import { pricingTrialDays } from "@/components/sections/pricing/pricing-tiers";
import { featureGroups } from "@/components/features/features";
import { Button } from "@/components/ui/button";

const columns = [
  {
    title: "Product",
    links: [
      {
        label: "Live demo",
        href: "https://app.kobbe.io/demo/kobbe-studio?range=30d",
      },
      { label: "All features", href: "/features" },
      { label: "Changelog", href: "/changelog" },
      { label: "Pricing", href: "/#pricing" },
    ],
  },
  {
    title: "Docs",
    links: [
      { label: "Get started", href: "/docs" },
      { label: "Add the tracker", href: "/docs/add-the-tracker" },
      { label: "Install guides", href: "/docs/installation-guides" },
      { label: "CLI and MCP", href: "/docs/cli" },
      { label: "Import your data", href: "/docs/import-analytics-data" },
    ],
  },
  {
    title: "Support",
    links: [
      { label: "Contact", href: "/support/contact" },
      { label: "FAQ", href: "/support/faq" },
      { label: "Charges", href: "/support/charges" },
      { label: "Billing and usage", href: "/docs/billing-and-usage-limits" },
    ],
  },
  {
    title: "Legal",
    links: [
      { label: "Privacy", href: "/legal/privacy" },
      { label: "Terms", href: "/legal/terms" },
      { label: "GDPR", href: "/legal/gdpr-compliance" },
      { label: "DPA", href: "/legal/dpa" },
    ],
  },
];
---

<footer class="">
  <Wrapper variant="default" class="pt-24 pb-8">
    <!-- Quiet CTA in the hero's two-tone voice -->
    <div
      class="flex flex-col gap-8 sm:flex-row sm:items-end sm:justify-between"
    >
      <h2
        class="max-w-2xl text-[clamp(1.5rem,2.5vw,2.25rem)] leading-tight tracking-tight text-balance"
      >
        <span class="text-foreground"
          >Find out what your traffic is actually worth.</span
        >{" "}<span class="text-muted-foreground"
          >Free for {pricingTrialDays} days, no credit card required.</span
        >
      </h2>
      <Button
        variant="solid"
        label={`Try free for ${pricingTrialDays} days`}
        href={APP_SIGNUP_URL}
      />
    </div>

    <div
      class="text-foreground bg-muted-surface mt-12 overflow-hidden rounded-xl p-8 lg:p-20"
    >
      <!-- Site links -->
      <div class="grid grid-cols-2 gap-8 sm:grid-cols-3 lg:grid-cols-5">
        <div>
          <a href="/" aria-label="Kobbe home">
            <Logo class="text-foreground size-8" />
          </a>
        </div>
        {
          columns.map((column) => (
            <div class="space-y-3">
              <p class="text-foreground text-sm font-medium">{column.title}</p>
              <ul class="space-y-2">
                {column.links.map((link) => (
                  <li>
                    <a
                      href={link.href}
                      class="text-muted-foreground hover:text-foreground text-sm transition-colors font-medium"
                    >
                      {link.label}
                    </a>
                  </li>
                ))}
              </ul>
            </div>
          ))
        }
      </div>

      <!-- Every feature, grouped the same way as /features -->
      <div class="border-border mt-8 border-t py-8">
        <div class="grid grid-cols-2 gap-8 sm:grid-cols-3 lg:grid-cols-5">
          {
            featureGroups.map((group) => (
              <div class="space-y-3">
                <p class="text-foreground text-sm font-medium">
                  {group.eyebrow}
                </p>
                <ul class="space-y-2">
                  {group.items.map((item) => (
                    <li>
                      <a
                        href={item.href}
                        class="text-muted-foreground font-medium hover:text-foreground text-sm transition-colors"
                      >
                        {item.title}
                      </a>
                    </li>
                  ))}
                </ul>
              </div>
            ))
          }
        </div>
      </div>

      <Wordmark class="text-foreground/20 select-none mt-12" aria-hidden="true" />

      <div
        class="text-muted-foreground border-border flex flex-wrap items-center justify-between gap-2 border-t pt-4 text-sm"
      >
        <span>© 2026 Kobbe. All rights reserved.</span>
        <span>Made in Åland Islands, Finland. EU</span>
      </div>
    </div>
  </Wrapper>
</footer>
